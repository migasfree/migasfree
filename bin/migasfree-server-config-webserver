#!/bin/bash

. $(cd $(dirname $0); pwd -P)/migasfree-server-functions

# main process

configure_web_app
if [ $? -ne 0 ]
then
    exit 1
fi

web_service restart

echo
echo "Web server configured. Test it at http://localhost/"
echo

exit 0
